<?xml version="1.0" encoding="UTF-8"?>
<logger mode="diagnostic">
    <file location="./LOG_MAIN/LOG_%DATE%_%TIME%_%USERNAME_%THREAD%.xml" formatter="xml" purpose="warehouse">
        <enter/>
        <exit/>
        <debug/>
        <info/>
        <warning/>
        <error>
            <exception formatter="exception_details_xml"/>
            <pan source="context"--not for here; only for inclusive logs; guid="dsfjgksdjfbg" mask="true" mask_settings="6,8,4"/>
            <all_from_context source="context" search="like" label="%" mask="true" --tries to mask supporting/>
        </error>
    </file>
</logger>

display=only static+explicit TODO????
todo: explicit locators and disabling traces (in addition to muting) TODO???????
mute=>disable? not needed
mask: see above - TODO
context- name fix? OK

make everything I_trace; enrich objects to I_trace via annotation; make guid mandatory - add it via annotation
GUIDs should be statis: classname+procedurename+trace name+occurence
guid should be on trace level

keep classname in addition to guid.
imaskable with mask settings as input parameter from this config

deduplication with SAME SOURCE